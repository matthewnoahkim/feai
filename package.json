{
  "name": "feai",
  "version": "1.0.0",
  "private": true,
  "description": "Web-based CAD Software with REST API",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "node ./node_modules/concurrently/dist/bin/concurrently.js \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev -w @feai/frontend",
    "dev:backend": "npm run dev -w @feai/backend",
    "build": "npm run build -w @feai/shared && npm run build -w @feai/kernel && npm run build -w @feai/backend && npm run build -w @feai/frontend",
    "build:shared": "npm run build -w @feai/shared",
    "build:kernel": "npm run build -w @feai/kernel",
    "build:backend": "npm run build -w @feai/backend",
    "build:frontend": "npm run build -w @feai/frontend",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "node ./node_modules/rimraf/dist/esm/bin.mjs packages/*/dist packages/*/.turbo node_modules/.cache"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/feai/feai.git"
  },
  "license": "MIT"
}
